# Docker Compose for AgriSense AI
version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_PORT=8501
    volumes:
      - ./agrisense_farmers.db:/app/agrisense_farmers.db
    restart: unless-stopped
